import java.util.Scanner;

public class SectorArea {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
 System.out.print("Enter the radius (r): ");
        double r = scanner.nextDouble();
        System.out.print("Enter the angle in radians (θ): ");
        double theta = scanner.nextDouble()
        double area = 0.5 * r * r * theta;
        System.out.println("The area of the sector is: " + area);
    }
}
